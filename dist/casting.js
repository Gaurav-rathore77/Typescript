"use strict";
let color = "green";
console.log(color.length); //A straightforward way to cast a variable is using the as keyword, which will directly change the type of the given variable.
let num1 = 4;
console.log(num1.length);
let x1 = 'hello';
console.log(x1.length); // Using <> works the same as casting with as.
// let x = 'hello';
// console.log(((x as unknown) as number).length)
